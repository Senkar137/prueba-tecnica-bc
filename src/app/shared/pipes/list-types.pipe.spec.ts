import { ListTypesPipe } from './list-types.pipe';

describe('ListTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new ListTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
